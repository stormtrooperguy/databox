#pragma once

// Copy this file to secrets.h (git-ignored) and set the venue AP credentials.
// The beacon joins this network as a CLIENT — the same AP the console and the
// portable devices are on.
#define WIFI_SSID     "YOUR_SSID"
#define WIFI_PASSWORD "YOUR_PASSWORD"
