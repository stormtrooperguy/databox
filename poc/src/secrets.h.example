#pragma once

// Copy this file to secrets.h (git-ignored) and set the AP credentials. They
// must match the databox reader's WiFi SSID/password so the reader connects.
#define AP_SSID     "YOUR_SSID"
#define AP_PASSWORD "YOUR_PASSWORD"
